{
  "name": "temp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "local": "PORT=3002 NODE_ENV=local nodemon ./bin/www",
    "run-local": "docker compose up -d",
    "run-local-build": "docker compose up --build -d"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "morgan": "~1.9.0",
    "pug": "^3.0.2"
  }
}
